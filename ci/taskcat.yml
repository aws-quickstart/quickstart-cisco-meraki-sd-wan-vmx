global:
  owner: simarbsi@cisco.com
  qsname: quickstart-cisco-meraki-sd-wan-vmx
  regions:
    - us-east-1
  reporting: true
tests:
  default:
    parameter_input: meraki_vmx_input.json 
    template_file: master.template.yaml
    regions:
      - us-east-1
